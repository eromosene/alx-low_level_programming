#include "main.h"

/**
 * swap_int - takes in two variables and swaps them
 * @author Daniel
 * @a: swaps and stores address of b
 * @b: swaps and stores address of a
 * Return: 0
 */

void swap_int(int *a, int *b)
{
int swap;
change = *b;
*b = *a;
*a = change;
}
